関数
１．SUM・・・数値の合計
２．AVERAGE・・・範囲内の平均
３．COUNTA・・・範囲内の個数
４．VLOOKUP・・・条件に一致している値
５．IF・・・条件に合わせて結果を表示
６．IFERROR・・・エラー時の表示設定

１～３では　＝SUM（　　のように入力してから範囲を指定する

４では=VLOOKUP($B2,Sheet11!$B2:$E2,2,FALSE) のように入力する
VLOOKUP(検索するセル,参照する範囲,範囲内の何個目を参照するか,FALSE）

６では=IFERROR(VLOOKUP($B2,Sheet11!$B2:$E2,2,FALSE),"")のように入力する
IFERROR(この関数がエラーかどうか,エラー発生時の処理)
ただし、IFERRORはエラーーが発生している前提で使用する関数のため使い勝手悪い。そのためIF関数の方がよく使う。